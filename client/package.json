{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.13.2",
    "jwt-decode": "^3.1.2",
    "moment": "^2.30.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.30.2",
    "react-scripts": "^0.0.0"
  },
  "scripts": {
    "start": "set \"NODE_OPTIONS=--no-webstorage\" && set \"DISABLE_ESLINT_PLUGIN=true\" && react-scripts start",
    "dev": "set \"NODE_OPTIONS=--no-webstorage\" && set \"DISABLE_ESLINT_PLUGIN=true\" && react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "tailwindcss": "^3.3.1"
  }
}
